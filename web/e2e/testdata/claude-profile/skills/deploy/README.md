# Deploy notes

The scanner flags the header below, which is what this fixture is for. The
value is the same throwaway string `internal/secretscan`'s own test uses.

    curl -H 'Authorization: bearer QmFzZTY0c2VjcmV0LWFldGhlci10ZXN0LTQy' https://example.invalid/deploy
